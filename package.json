{
  "name": "jekyll-vercel",
  "version": "1.0.0",
  "scripts": {
    "build": "bundle install && bundle exec jekyll build"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "jq": "^1.7.2"
  }
}