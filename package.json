{
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "jq": "^1.7.2"
  },
  "scripts": {
    "build": "bundle install && bundle exec jekyll build"
  },
  "devDependencies": {
    "jekyll": "^4.2.0"
  },
  "engines": {
    "node": ">=14.x"
  }
}