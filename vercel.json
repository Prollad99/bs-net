{
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "bundle exec jekyll build",
        "distDir": "_site"
      }
    }
  ]
}